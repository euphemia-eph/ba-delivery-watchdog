{
  "name": "visibility-delivery-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Refreshes a Brand Alchemy client-health dashboard every weekday morning from ClickUp, Slack, and call notes, scores each account, and serves the front-end against live data.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "sync": "node scripts/sync.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3"
  }
}
